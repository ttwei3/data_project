<!-- app/views/breeds/index.html.erb -->
<h1>Breeds</h1>

<ul>
  <% @breeds.each do |breed| %>
    <li>
      <%= breed.breed_name %>
      <%= image_tag breed.image, size: '200x200' if breed.image.present? %>
    </li>
  <% end %>
</ul>